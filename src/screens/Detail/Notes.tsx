import {View, Text, ScrollView} from 'react-native';
import React from 'react';

const Notes = () => {
  return (
    <ScrollView>
      <View>
        <Text>Notes</Text>
      </View>
    </ScrollView>
  );
};

export default Notes;
